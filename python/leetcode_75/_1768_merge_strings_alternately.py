class Solution:
    def mergeAlternately(self, word1: str, word2: str) -> str:
        final_world_list = []
        for letter_word1 in word1:
            for letter_word2 in word2:
                
        return "".join(final_world_list)

Solution().mergeAlternately("abc", "pqr")